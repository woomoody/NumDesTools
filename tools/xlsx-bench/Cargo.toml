[package]
name = "xlsx-bench"
version = "0.1.0"
edition = "2024"

[dependencies]
calamine = "0.26"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
quick-xml = "0.31"
zip = { version = "2", default-features = false, features = ["deflate"] }

[profile.release]
opt-level = 3
