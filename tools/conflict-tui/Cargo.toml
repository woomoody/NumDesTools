[package]
name = "conflict-tui"
version = "0.1.0"
edition = "2021"

[dependencies]
ratatui = "0.26"
crossterm = "0.27"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
similar = "2"  # 字符级 LCS diff，比手写 DP 简单
