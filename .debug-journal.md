# Debug Journal — XlsxEditor copy/paste row height
Started: 2026-07-31
Goal: Confirm and fix paste row-height growth, range copy, and copied-state visibility without changing excluded files.

## Environment snapshot
- Runtime: .NET 9 WPF, launcher `NumDesTools.XlsxEditor.exe <xlsx-path>`
- Entry: `NumDesTools.XlsxEditor/App.xaml.cs` passes the first CLI argument to `MainWindow.LoadFile`
- Existing repro: `C:/M1Work/Public/Excels/Tables/ActivityBalloon.xlsx`
- References read: debugging methodology `00-setup.md`, `02-investigate.md`

## Hypotheses
1. [OPEN] Paste updates RowView while the target cell still retains an editing TextBox measured with Wrap; unlike normal commit, paste never commits the edit nor invalidates the affected rows. Distinguishing evidence: target row ActualHeight/DesiredSize and child TextBox/TextBlock before and after paste versus normal edit. If true, fix is: commit + remeasure.
2. [OPEN] Shift-click does not reach the range branch or its state is replaced before Ctrl+C. Distinguishing evidence: runtime clipboard and existing DebugLog `_selectionKind` after a physical Shift-click. If true, fix is: modifier detection/state ordering.
3. [OPEN] Copied-state code runs but DataGridCell.Foreground does not determine the generated TextBlock's effective foreground. Distinguishing evidence: DataGridCell and TextBlock Foreground value sources after copy. If true, fix is: content foreground.
4. [OPEN] Running binary is stale relative to source. Distinguishing evidence: debug log signatures and live visual tree behavior. If true, fix is: rebuild/relaunch.

## Artifacts to revert
- [ ] This `.debug-journal.md`; delete after final evidence is captured.
- [ ] Launched `NumDesTools.XlsxEditor.exe` process; close after QA.
- [ ] Existing `C:/Users/cent/AppData/Local/Temp/opencode/xlsx_debug.log`; user explicitly requires deletion after fix.

## Findings
- Existing log observed `Copy enter kind=SingleCell` for every copy attempt and `ApplyMarqueeBorder exit applied=1`, proving copy-state rendering executes while the selection state remains single-cell.

## Final fix
- Pending user approval after root-cause confirmation.
