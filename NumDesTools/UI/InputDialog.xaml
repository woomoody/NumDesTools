<ui:FluentWindow
    x:Class="NumDesTools.UI.InputDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    SizeToContent="WidthAndHeight"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    KeyDown="Window_KeyDown"
    WindowBackdropType="Mica"
    ExtendsContentIntoTitleBar="True"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ui:TitleBar Grid.Row="0" />
        <StackPanel Grid.Row="1" Margin="16">
            <ui:TextBlock x:Name="PromptText" TextWrapping="Wrap" FontSize="13"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
            <ui:TextBox x:Name="InputTextBox" MinWidth="320" Margin="0,10,0,0" Visibility="Collapsed"/>
            <PasswordBox x:Name="InputPasswordBox" MinWidth="320" Margin="0,10,0,0"
                         Visibility="Collapsed" PasswordChar="*"/>
            <ui:Button HorizontalAlignment="Right" IsDefault="True" Margin="0,10,0,0" Click="OkButton_Click" Content="确定"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>