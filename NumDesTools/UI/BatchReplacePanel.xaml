<UserControl
    x:Class="NumDesTools.UI.BatchReplacePanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    x:Name="Root">

    <Grid Background="{DynamicResource ApplicationBackgroundBrush}" KeyDown="Grid_KeyDown">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" MinWidth="100"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ══ 左侧历史 ══ -->
        <Grid Grid.Column="0" Background="{DynamicResource LayerFillColorDefaultBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ui:TextBlock Grid.Row="0" Text="历史记录"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       FontSize="11" Margin="8,8,8,4"/>
            <ListBox x:Name="HistoryList" Grid.Row="1"
                     Background="Transparent" BorderThickness="0"
                     SelectionChanged="HistoryList_SelectionChanged"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Padding" Value="6,4"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlFillColorInputActiveBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentFillColorDefaultBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ui:TextBlock Grid.Column="0" Text="{Binding Display}" TextTrimming="CharacterEllipsis"
                                       FontSize="11" ToolTip="{Binding Detail}" VerticalAlignment="Center"/>
                            <ui:Button Grid.Column="1" Content="✕" FontSize="9"
                                    Background="Transparent" BorderThickness="0"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Padding="4,1" Margin="4,0,0,0"
                                    Tag="{Binding}"
                                    Click="DeleteHistory_Click"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <ui:Button Grid.Row="2" Content="清空历史"
                    Background="{DynamicResource LayerFillColorDefaultBrush}"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                    Margin="6,4,6,6" Padding="10,4" Click="ClearHistory_Click"/>
        </Grid>

        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"
                      Background="{DynamicResource ControlElevationBorderBrush}"/>

        <!-- ══ 右侧编辑区 ══ -->
        <Grid Grid.Column="2" Margin="10,8,10,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ui:TextBlock Grid.Row="0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="11"
                       Margin="0,0,0,6" TextWrapping="Wrap"
                       Text="每行一对：查找值 → 替换值（Ctrl+Enter 执行）"/>

            <!-- 列头 -->
            <Grid Grid.Row="1" Margin="0,0,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="28"/>
                </Grid.ColumnDefinitions>
                <ui:TextBlock Grid.Column="0" Text="查找值"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           FontSize="11" HorizontalAlignment="Center"/>
                <ui:TextBlock Grid.Column="2" Text="替换值"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           FontSize="11" HorizontalAlignment="Center"/>
            </Grid>

            <!-- 规则行 -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl x:Name="RuleRows">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="28"/>
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0">
                                    <ui:TextBox x:Name="FromBox"
                                             Background="{DynamicResource ControlFillColorInputActiveBrush}"
                                             Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                             BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                                             CaretBrush="{DynamicResource TextFillColorPrimaryBrush}"
                                             SelectionBrush="{DynamicResource AccentFillColorDefaultBrush}"
                                             BorderThickness="1" Padding="4,3"
                                             Text="{Binding From, UpdateSourceTrigger=PropertyChanged}"
                                             FontFamily="Consolas"/>
                                    <ui:TextBlock IsHitTestVisible="False" Padding="6,4"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                               FontFamily="Consolas" FontStyle="Italic"
                                               Text="查找值...">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding From}" Value="">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </ui:TextBlock>
                                </Grid>
                                <ui:TextBlock Grid.Column="1" Text="→" VerticalAlignment="Center"
                                           HorizontalAlignment="Center"
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                <ui:TextBox Grid.Column="2"
                                         Background="{DynamicResource ControlFillColorInputActiveBrush}"
                                         Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                         BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                                         CaretBrush="{DynamicResource TextFillColorPrimaryBrush}"
                                         SelectionBrush="{DynamicResource AccentFillColorDefaultBrush}"
                                         BorderThickness="1" Padding="4,3"
                                         Text="{Binding To, UpdateSourceTrigger=PropertyChanged}"
                                         FontFamily="Consolas"/>
                                <ui:Button Grid.Column="3" Content="✕" FontSize="10" Margin="2,0,0,0"
                                        Background="{DynamicResource LayerFillColorDefaultBrush}"
                                        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                        BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                                        Padding="4,3" Tag="{Binding}" Click="RemoveRow_Click"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <ui:Button Grid.Row="3" Content="+ 添加一行"
                    Background="{DynamicResource LayerFillColorDefaultBrush}"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                    HorizontalAlignment="Left" Margin="0,6,0,6" Padding="8,3"
                    Click="AddRow_Click"/>

            <!-- 状态 + 按钮 -->
            <Grid Grid.Row="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ui:TextBlock x:Name="StatusText" Grid.Row="0" FontSize="11" Margin="0,0,0,6"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <ui:Button Content="执行替换 (Ctrl+Enter)"
                            Background="{DynamicResource AccentFillColorDefaultBrush}"
                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                            BorderBrush="{DynamicResource AccentFillColorDefaultBrush}"
                            BorderThickness="1" Padding="10,4"
                            Width="150" Margin="0,0,8,0" Click="Execute_Click"/>
                    <ui:Button Content="清空"
                            Background="{DynamicResource LayerFillColorDefaultBrush}"
                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                            BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                            Padding="10,4" Width="60" Click="Clear_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>