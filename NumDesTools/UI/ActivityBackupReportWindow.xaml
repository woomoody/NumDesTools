<mah:MetroWindow
    x:Class="NumDesTools.UI.ActivityBackupReportWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    Title="大文件备份"
    Width="700" Height="500" MinWidth="500" MinHeight="350"
    WindowStartupLocation="CenterScreen"
    KeyDown="Window_KeyDown">

    <Grid Margin="14,12,14,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="BodyText" Grid.Row="0"
                 IsReadOnly="True" IsReadOnlyCaretVisible="True"
                 TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="13"
                 BorderThickness="1" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="取消" Width="90" Height="32" Margin="0,0,10,0"
                    Click="Cancel_Click"/>
            <Button x:Name="OkButton" Content="确定" Width="90" Height="32" IsDefault="True"
                    Style="{DynamicResource MahApps.Styles.Button.Square.Accent}"
                    Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</mah:MetroWindow>
