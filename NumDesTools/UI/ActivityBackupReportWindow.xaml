<ui:FluentWindow
    x:Class="NumDesTools.UI.ActivityBackupReportWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="大文件备份"
    Width="700" Height="500" MinWidth="500" MinHeight="350"
    WindowStartupLocation="CenterScreen"
    KeyDown="Window_KeyDown"
    WindowBackdropType="Mica"
    ExtendsContentIntoTitleBar="True">

    <Grid Margin="14,12,14,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TextBox x:Name="BodyText" Grid.Row="0"
                 IsReadOnly="True" IsReadOnlyCaretVisible="True"
                 TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="13"
                 BorderThickness="1" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <ui:Button x:Name="CancelButton" Content="取消" Width="90" Height="32" Margin="0,0,10,0"
                    Click="Cancel_Click"/>
            <ui:Button x:Name="OkButton" Content="确定" Width="90" Height="32" IsDefault="True"
                    Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
