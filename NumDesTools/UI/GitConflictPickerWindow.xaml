<ui:FluentWindow
    x:Class="NumDesTools.UI.GitConflictPickerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Git 冲突解决"
    MinWidth="420" MinHeight="260"
    SizeToContent="Height"
    MaxHeight="600"
    WindowStartupLocation="CenterScreen"
    KeyDown="Window_KeyDown"
    WindowBackdropType="Mica"
    ExtendsContentIntoTitleBar="False">

    <Grid Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 冲突文件列表 -->
        <ListBox x:Name="FileList"
                 Grid.Row="0"
                 MinHeight="80"
                 FontFamily="Consolas" FontSize="11"
                 BorderThickness="0"
                 Background="#1E1E1E"
                 Foreground="#FFFFFF"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 SelectionMode="Single"
                 MouseDoubleClick="FileList_MouseDoubleClick"
                 KeyDown="FileList_KeyDown">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="8,3"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <!-- 底部操作区 -->
        <Border Grid.Row="1"
                BorderBrush="#2D2D2D"
                BorderThickness="0,1,0,0"
                Padding="10,8">
            <StackPanel>
                <!-- 当前分支（OURS）选择 -->
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ui:TextBlock Text="当前分支（OURS）：" VerticalAlignment="Center"
                               Foreground="Gray"
                               FontSize="11" Margin="0,0,6,0"/>
                    <ComboBox x:Name="BranchBox" Grid.Column="1"
                              FontFamily="Consolas" FontSize="11"/>
                </Grid>
                <CheckBox x:Name="SkipHashBox"
                          Content="跳过 # 文件（以对方为准自动解决）"
                          Foreground="Gray"
                          FontSize="11"
                          Margin="0,0,0,8"
                          Checked="SkipHash_Changed"
                          Unchecked="SkipHash_Changed"/>
                <ui:Button x:Name="BatchAutoBtn"
                        Content="⚡ 一键自动解决（扫描可预选文件）"
                        HorizontalAlignment="Stretch"
                        Padding="0,8"
                        Margin="0,0,0,6"
                        Click="BatchAuto_Click"/>
                <ui:Button x:Name="ResolveBtn"
                        Content="解决选中文件"
                        HorizontalAlignment="Stretch"
                        Padding="0,8"
                        Click="Resolve_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</ui:FluentWindow>
