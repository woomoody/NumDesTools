<Window x:Class="NumDesTools.FileLockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="文件占用" Height="280" Width="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Width" Value="80" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Margin" Value="5,0" />
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="文件正被占用，无法保存：" FontWeight="Bold" Margin="0,0,0,8" />

        <TextBox Grid.Row="1" x:Name="FilePathBox" IsReadOnly="True" Background="#F5F5F5"
                 TextWrapping="Wrap" Margin="0,0,0,8" />

        <GroupBox Grid.Row="2" Header="占用进程" Margin="0,0,0,12">
            <ListBox x:Name="ProcessListBox" Margin="4" />
        </GroupBox>

        <StackPanel Grid.Row="3" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Content="确定" IsDefault="True" Click="OkButton_Click" />
        </StackPanel>
    </Grid>
</Window>