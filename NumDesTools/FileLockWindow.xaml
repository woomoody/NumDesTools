<ui:FluentWindow
    x:Class="NumDesTools.FileLockWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="文件占用"
    Height="280" Width="520"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    KeyDown="Window_KeyDown"
    WindowBackdropType="Mica"
    ExtendsContentIntoTitleBar="True"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ui:TitleBar Grid.Row="0" Title="文件占用" />
        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ui:TextBlock Grid.Row="0" Text="文件正被占用，无法保存："
                       FontWeight="Bold" Margin="0,0,0,8"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />

            <ui:TextBox Grid.Row="1" x:Name="FilePathBox" IsReadOnly="True"
                     Background="{DynamicResource ControlFillColorInputActiveBrush}"
                     Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                     TextWrapping="Wrap" Margin="0,0,0,8" />

            <GroupBox Grid.Row="2" Header="占用进程" Margin="0,0,0,12"
                      Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                <GroupBox.Style>
                    <Style TargetType="GroupBox">
                        <Setter Property="BorderBrush" Value="{DynamicResource ControlElevationBorderBrush}" />
                        <Setter Property="Background" Value="{DynamicResource ApplicationBackgroundBrush}" />
                    </Style>
                </GroupBox.Style>
                <ListBox x:Name="ProcessListBox" Margin="4"
                         Background="{DynamicResource ApplicationBackgroundBrush}"
                         Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            </GroupBox>

            <StackPanel Grid.Row="3" HorizontalAlignment="Right" Orientation="Horizontal">
                <ui:Button Content="确定" IsDefault="True" Click="OkButton_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>